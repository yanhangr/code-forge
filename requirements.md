# Code Forge：Agent Runtime 架构需求基线

更新：2026-09-10。状态：用户已确认方向；当前由架构师交付核心代码、表结构和设计契约，具体适配器与应用交由其他智能体实现。本文件取代早期一期沙箱、完整 Platform 管理产品和 Runtime 角色拆分部署的建议。

## 1. 目标

建设可服务化运行的通用 Agent Runtime，使用 Deep Agents/LangGraph 与自研运行层，实现通用对话、Coding、Python 数据分析、外部工具/MCP 与报告生成。用户拥有连续会话和持久文件，底层逐步形成共享集群运行能力。

当前重点是验证 Agent 的运行能力：真实模型能理解任务、选择工具、执行 Python/文件操作、读取反馈、完成工作并在下一轮继续。Platform 只用于观察和验证主流程，不能让管理产品开发先阻塞 Runtime。

## 2. 用户已确认的决定

| 编号 | 决策 |
| --- | --- |
| D1 | 首版目标包括通用对话、完整 Coding、数仓分析与图表/报告生成；交互终端体验后置 |
| D2 | Python 在 Agent 所在机器以子进程执行；保留 ExecutionBackend，后续接 SandboxBackend |
| D3 | 文件持久；不保证进程和 Python 内存变量长期存活 |
| D4 | Deep Agents/LangGraph + 自研 Runtime，不使用付费 Agent Server/托管 Agent 产品 |
| D5 | Skill 在 Run 接受时锁定版本及包内容，执行中按需加载；新 Run 使用新的绑定结果 |
| D6 | 一期两个应用：platform-app、agent-runtime；Runtime 内部模块化、合并部署，后续按负载拆分 |
| D7 | Runtime 优先开发，Platform 只实现最小主流程验证页面，二者接口必须清晰稳定 |
| D8 | Skill 先手工编辑；默认有权限。Skill 编辑发布、权限后台、审批、文件/报告展示后续完善 |
| D9 | 身份、角色和业务授权属于上层；Runtime 通过通用接口接收上下文并落实执行机制 |
| D10 | 当前职责为架构把控：交付核心代码、表结构、流程/状态设计、详细文档和实现交接；不包办应用全部实现 |

## 3. 当前两应用范围

| 应用 | 当前需要 | 后续建设 |
| --- | --- | --- |
| agent-runtime | 接受/调度/执行/查询/取消 Run；Deep Agents、工具、Python 子进程、持久会话、手工 Skill 快照、事件、文件结果 | 经过验证的多副本 HA、完整外部集成、沙箱、更细治理与扩展能力 |
| platform-app | 创建/选择会话、提交输入/Skill 选择、观察消息和工具过程、取消、刷新重连、继续对话/必要澄清 | Skill 编辑发布、权限/角色、审批工作流、文件管理与报告预览、完善产品 UI |

高可用仍是架构目标，但验证必须分层报告：核心代码测试、数据库/执行器集成、框架链路、真实模型主流程、多副本故障验证不得混用。两个应用可以各自多副本；先单副本验证不等于 HA 已交付。

数据库、文件存储、模型和数仓是基础设施/依赖，不计为额外自研业务应用。Runtime 的 API、Scheduler、Worker、Reconciler 一期同一构建/部署单元，状态仍持久化，不依赖内存队列和 Sticky Session。

## 4. 核心行为约束

- Session 是连续会话，Run 是一次输入，Attempt 是一次执行/接管。普通追问创建新 Run，澄清回应恢复原 Run。
- 返回“已接受”之前，输入、配置快照、队列记录和接受事件可靠提交。同键相同请求复用 Run；不同内容冲突。
- 同 Session 主 Run 串行，同 Workspace 一个合法 writer；等待主任务不能被后序消息越过。
- Skill/Agent/模型/工具/执行 profile 在接受时固定；Workspace 起点在实际执行时固定，确保看到前序成果。
- 关键状态更新有 state_version CAS；故障接管有 epoch 与检查点写入校验。终态不原地复活。
- 工具逻辑操作意图先持久化，再执行。外部效果未知不能盲重试；Runtime 重启不能简单重放整轮用户输入。
- 取消是显式动作，页面断线不取消；取消不自动撤销已生成文件或远端副作用。
- Run 正常结束与任务目标完成区分。模型文字、HTTP 202 或测试替身不能代替真实执行/测试证据。

## 5. Coding、Python 与执行后端

完整 Coding 目标包括理解仓库、项目指令、搜索与修改、多文件变更、构建测试、依据错误修复、Git/Diff 与可追溯成果。交互终端 UI 后置，Agent 执行命令能力保留。

一期 LocalProcessBackend 使用子进程、明确 cwd/argv、超时、输出上限、进程组取消、状态查询与结果收集。每个 Attempt 独立暂存目录，正式文件成果提交后才标已保存。不在 Worker 进程中直接 eval/exec 用户/模型 Python。

这是可信内部默认授权验证模式。本机子进程与宿主共享权限，不作为沙箱或强多租户隔离声明；后续 SandboxBackend 通过相同操作契约接入。生产开放前的真实授权和隔离验收单独安排。

工具/脚本失败应保留退出码、stderr、输入文件修订和实际环境；未执行的测试明确未验证。测试后代码改变需重测或标注证据已过期。

## 6. 手工 Skill 与未来管理接口

手工目录包含 SKILL.md 和可选脚本/资料；每包记录 name/version/digest，按实际读取字节生成不可变快照。运行时从快照加载，源目录修改只影响新 Run，不改变已接受任务。

只读取本 Run 指定的候选摘要，按需加载正文与支持文件。空候选列表不自动加载全部 Skill。快照覆盖脚本和资源，不只记录 Markdown 或版本号。

当前无需发布数据库、编辑器或审批。ManualSkillResolver 为 SnapshotResolver 提供实现；未来 Platform 管理注册中心后，换接 Platform 版本解析器，运行契约与旧快照仍可使用。不能同时让两应用维护两套可变发布通道。

## 7. Platform 接口与默认权限

平台只使用版本化 REST/SSE 接口，不读 Runtime 数据库或工作区路径，不直接运行 Agent/Python。当前用显式 DefaultAllowAuthorization，无业务登录或审批流程。

保留 ExecutionContext、AuthorizationPort 和后端 profile 等替换边界；用户/项目角色不进入核心领域依赖。默认 scope=default，actor_ref/external_ref 只作关联，默认授权模式不能被描述成生产认证。

公开字段、状态、错误码与事件 payload 由 [OpenAPI](docs/api/openapi.json) 固定，[接入语义](docs/api/platform-runtime.md) 解释幂等、等待、取消、游标与重连。实现智能体不得自行重命名或引入未记录的约定。

## 8. 主流程与后续验收

| 编号 | 场景 | 验收结果 |
| --- | --- | --- |
| A01 | 核心状态转换 | 非法边/终态复活被拒绝，取消幂等，state_version 冲突可识别 |
| A02 | 重复/并发提交 | 同请求只产生一个 Run，同键不同内容冲突 |
| A03 | 持久接受 | 202 之后服务重启仍能读取输入、快照与任务 |
| A04 | Skill 更新 | 修改手工文件后，旧 Run 包不变，新 Run 按新内容解析 |
| A05 | 真 Python 执行 | 计算结果、退出码、日志、生成文件和模型回复一致 |
| A06 | Coding 闭环 | 真实仓库修改/构建/测试/修复及 Diff，保留已有用户修改 |
| A07 | 进程管理 | 超时/取消处理子孙进程，大输出不堵住 API/心跳 |
| A08 | 结果未知 | 进程中断不盲重放，核验后才恢复或明确失败/等待 |
| A09 | 多轮连续 | 同会话下一轮看到前轮持久文件和有效上下文 |
| A10 | 队列顺序 | 同 Session 无并发主写入，等待/普通追问语义清楚 |
| A11 | 事件恢复 | 断线不取消，游标补发去重，最终文本不重复追加 |
| A12 | Platform 解耦 | 最小页面仅通过 HTTP/SSE 验证，不依赖 Runtime 私有实现 |
| A13 | 授权接口 | 当前默认允许，可替换拒绝适配器，核心无角色库依赖 |
| A14 | 外部工具/数仓 | fixture 与真实接入分别记录，数据来源/查询结果可核验 |
| A15 | 多副本与升级 | 后续专项验证 lease/epoch、旧写入、兼容检查点和恢复，未测不称 HA 完成 |
| A16 | 沙箱 | 后续更换后端并验证隔离，非本轮实现要求 |

## 9. 流程、规范与数据库约定

完整工作流程按 B01—B48 分支定义，明确 LLM、Deep Agents、LangGraph、自研Runtime各阶段职责，并逐项提供测试前置/动作/断言。案例定义完整不等于集成已通过。

目录/依赖/设计/异步/错误/契约演进遵循 docs/coding-standards.md。自管PG表统一 date_created/created_by/date_updated/updated_by；表及全部字段有COMMENT；索引仅主键、普通B-tree、唯一索引，不使用条件/表达式等特殊索引。活动写入者通过指针与事务保证。

## 10. 架构交付与实现责任

本轮已提供核心领域/状态/接受顺序代码及相应测试；HTTP、数据库/执行/框架适配器与页面由实现智能体完成。SQL 文件是数据库结构设计，尚未部署；OpenAPI 是可生成客户端的契约，尚无对应在线服务。

实施入口为 [详细设计](docs/detailed-design.md) 和 [智能体交接说明](docs/implementation-guide.md)。变更字段、表结构、状态或可靠性语义须先反馈架构，并同步规范、代码、SQL、生成物与测试，避免两个实现者各自解释。

[第一轮评审与确认记录](docs/requirements-review.md) 和 [最初方案](docs/requirements-original.md) 保留历史依据；与本基线冲突的历史建议不再生效。
